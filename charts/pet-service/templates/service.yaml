apiVersion: v1
kind: Service
metadata:
  name: pet-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: pet-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP