apiVersion: v1
kind: Service
metadata:
  name: frontend-web-active

spec:
  type: {{ .Values.service.type }}
  selector:
    app: frontend-web

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP